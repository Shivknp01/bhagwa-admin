import React from "react";

export default function CommentModerationPage() {
  return (
    <div className="space-y-4">
      <h1 className="text-2xl font-bold text-[var(--text-primary)]">Comment Moderation</h1>
      <p className="text-sm text-[var(--text-secondary)]">Moderate user comments, filter spam, hide inappropriate text, and warn users.</p>
      <div className="p-8 border border-dashed border-[var(--border-color)] rounded-2xl text-center text-[var(--text-secondary)]">
        Comment Moderation Table Placeholder (Phase 10)
      </div>
    </div>
  );
}
